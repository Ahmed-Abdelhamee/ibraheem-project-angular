import { SizeReaderPipe } from './size-reader.pipe';

describe('SizeReaderPipe', () => {
  it('create an instance', () => {
    const pipe = new SizeReaderPipe();
    expect(pipe).toBeTruthy();
  });
});
